name: counter_app
description: A simple chore tracking app for Roy
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_spinkit: ^5.2.0 # לאנימציות טעינה קלות
  shared_preferences: ^2.2.3

flutter:
  uses-material-design: true